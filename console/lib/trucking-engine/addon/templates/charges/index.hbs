<Layout::Resource::Tabular
    @resource="trucking-charge"
    @title="Charges"
    @searchQuery={{this.query}}
    @data={{@model}}
    @columns={{this.columns}}
    @page={{this.page}}
    @onPageChange={{fn (mut this.page)}}
    @setupTable={{fn (mut this.table)}}
    @actionButtons={{this.actionButtons}}
    @controller={{this}}
/>
{{outlet}}
